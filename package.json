{
  "name": "painless-forms-with-formik",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "formik": "^0.10.4",
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "spectacle-code-slide": "^0.5.1",
    "spectacle-scripts": "2.0.0",
    "surge": "^0.19.0",
    "yup": "^0.23.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn build && surge -p build"
  }
}
